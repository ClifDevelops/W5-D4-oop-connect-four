class Column {
    constructor(){    
    }
    add (playerNumber){//playerNumber === 1/2
       for (let i = col.length - 1; i > -1; i--){//change 6 to current column length;
        if (currentCol[i] === null){
            //drop token here
            //return 
            
        }
       } 
    }
    getTokenAt(){
        //return 1/2/null
    }
}